# Homebrew formula for [TMSU](https://github.com/oniony/TMSU)

To install either run

`brew install --HEAD 'v1max/homebrew-tmsu/tmsu'`

or `brew tap v1max/homebrew-tmsu` and then `brew install --HEAD tmsu`.
